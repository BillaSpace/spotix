services:
  - type: web
    name: spotix
    env: node
    region: singapore
    plan: pro plus
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    autoDeploy: true
    branch: main 